enum GenderType { male, female, unknown }

enum SpeciesType {
  human,
  demon,
  meranth,
  fundamental,
  celestial,
  creatureOfDarkness
}

enum Position { carry, mid, offlane, support, hardSupport }

enum Attribute { strength, agility, intelligence, universal }

enum Range { melee, range }

enum Complexity { easy, medium, hard }
